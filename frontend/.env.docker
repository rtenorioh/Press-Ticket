# Ambiente: development ativa o Helmet para Security Headers (sem Nginx no Docker local)
NODE_ENV=development

# Porta do servidor frontend
PORT=3000

# URL do backend - necessária para configurar o CSP (frame-src, connect-src, img-src)
REACT_APP_BACKEND_URL=http://localhost:8000
