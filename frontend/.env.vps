# Ambiente: production — Security Headers gerenciados pelo Nginx
# NODE_ENV=production — Vite 6 não suporta esta variável em .env (vite build define automaticamente)

# Porta do servidor frontend
PORT=3000
